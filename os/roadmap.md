# Roadmap

Now / next / later, plus open flags. Keep lines short — detail lives in area pages and
incubator files. Lines starting `FLAG:` are surfaced into STATUS.md by
`os/scripts/gen-status.sh`; use them for open questions that must not get lost.

## Now

- Make the template yours: first real area in areas/, first real /sweep.

## Next

- Wire up a first integration (os/playbooks/add-integration.md), read-only scope.

## Later

- Satellites, if a project outgrows this repo — list their paths in os/satellites.txt.

## Open flags

FLAG: replace the example content (areas/example-project, incubator/example-idea.md, inbox/welcome-note.md) once real content exists.
