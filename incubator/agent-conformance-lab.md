# agent-conformance-lab

status: seed

**Pitch:** a heavy harness that runs the same tasks across different agent runtimes and
proves the 10 laws hold identically in each — catches a runtime that reads MAP.md
correctly but ignores the autonomy table.
