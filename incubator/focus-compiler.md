# focus-compiler

status: seed

**Pitch:** a `focus <area>` command that compiles a bounded working capsule — just one
area's pages, current state, and open flags — instead of the full read path. Estimated
3-5 days; not load-bearing at template scale, where MAP.md already does this cheaply.
