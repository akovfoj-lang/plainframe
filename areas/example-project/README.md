# Example project

A placeholder area showing the shape of an area page — replace it with your first real area.

**Goal:** make writing the first real area page effortless, by example.

**Current state:** template demo. Nothing real lives here.

**Next:** owner creates a real area from _templates/area.md, then deletes this folder
(or moves it to archive/ if the shape notes still help).

**Pointers:**

- Large files for this project live outside git — see [assets.md](assets.md). That page
  is a working demo of law 4: the assets are invisible to git, but never invisible to
  the system.
